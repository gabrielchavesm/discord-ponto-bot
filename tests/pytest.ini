[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    -m "not integration"
markers =
    integration: Integration tests requiring real database
    unit: Unit tests (deselect with '-m "not integration"')